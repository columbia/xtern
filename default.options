DMT = 1
schedule_network  = 1

runtime_type = RR
scheduler_seed = 0x12345 # seed for seeded round-robin scheduler
log_type = txt      # bin: binary log of instructions
                    # txt: text log of synchronizations
output_dir = ./out  # default output directory
